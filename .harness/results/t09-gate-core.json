{
  "task": "t09-gate-core",
  "kind": "gate",
  "status": "pass",
  "attempts": 1,
  "artifacts": [
    "tests/conftest.py",
    "tests/fixtures/fto43.smi",
    "tests/test_integration.py",
    "README.md"
  ],
  "smoke": {
    "ran": true,
    "ok": true,
    "detail": "pixi run pytest tests/ -m \"not model\" -q => 148 passed (130 baseline + 18 new integration/fixture tests); --strict-markers clean; import core + registry_validate() green after pip install -e ."
  },
  "note": "Core API frozen: integration tests pin models/schemas/registry/dispatch/run coherence; fast suite green. NEEDS_AARAN follow-up (does not gate this task): tests/fixtures/fto43.smi holds a documented PLACEHOLDER SMILES; the canonical CID-164886650 structure is a live PubChem lookup denied in this headless run (no-fabricate). One-line data swap, no code; only affects the opt-in @pytest.mark.model tier.",
  "commit": "task/t09-gate-core"
}
